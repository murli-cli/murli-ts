# pnpm 11 reads ONLY auth/registry settings from .npmrc; every other setting
# lives in pnpm-workspace.yaml. (npm also reads this during `pnpm publish`.)

# Pin the registry explicitly (dependency-confusion / registry-swap hygiene).
registry=https://registry.npmjs.org/
